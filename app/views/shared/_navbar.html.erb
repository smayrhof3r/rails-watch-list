<nav class="navbar sticky-top bg-dark text-light text-lg p-2 d-flex justify-content-between">
  <a href="<%= lists_path %>" class = "p-0 m-0 text-decoration-none text-light"><i class="fa fa-home-alt"></i></a>
  <% if @list && @list.persisted? %>
  <a href="<%= new_list_bookmark_path(@list)%>" class = "p-0 m-0 text-decoration-none text-light"><i class="fas fa-plus"></i> Movie</a>
  <% else %>
  <a href="<%= new_list_path%>" class = "p-0 m-0 text-decoration-none text-light"><i class="fas fa-plus"></i> List</a>
  <% end %>
</nav>
